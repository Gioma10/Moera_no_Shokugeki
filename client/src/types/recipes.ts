export interface RecipeFormData {
  image: string;
  title: string;
  description: string;
  // category: string;
  // ingredients: string;
  // preparations: string;
  // rating: number;
  // preparationTime?: string;
  // cookingTime?: string;
}