export interface RecipeFormData {
  id?: string;
  image: File;
  title: string;
  description: string;
  // category: string;
  // ingredients: string;
  // preparations: string;
  // rating: number;
  // preparationTime?: string;
  // cookingTime?: string;
}